version: '3.8'

services:
  gateway:
    build: .
    container_name: gateway
    ports:
      - "8080:8080"
    networks:
      - bookstore-network

networks:
  bookstore-network:
    name: bookstore-network